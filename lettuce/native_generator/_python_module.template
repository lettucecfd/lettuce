import torch
import os

# on windows add cuda path for
# native module to find all dll's
if os.name == 'nt':
    os.add_dll_directory(os.path.join(os.environ['CUDA_PATH'], 'bin'))

import {module}

{py_buffer}

def resolve(stencil, streaming, collision):

    name = f"{{stencil.name}}_{{streaming.name}}_{{collision.name}}"
    if name in globals():
        return globals()[name]

    return None
